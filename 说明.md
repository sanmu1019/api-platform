# 项目结构说明

```text
api-platform/
├── main.py                     # FastAPI 应用入口
├── admin/
│   └── route.py                # 后台管理接口与后台页面
├── apis/
│   ├── main.py                 # 路由注册入口
│   ├── data_loader.py          # txt/json 数据加载器
│   ├── versioned.py            # /api/v1 别名路由
│   ├── data/                   # 静态数据文件
│   │   ├── yiyan.txt
│   │   ├── words.txt
│   │   ├── nickname_prefixes.txt
│   │   ├── nickname_suffixes.txt
│   │   ├── phone_map.json
│   │   ├── phone_prefix.json
│   │   ├── spider.json
│   │   ├── history_today.json
│   │   ├── idioms.json         # 成语（释义已补全引号与转码符号）
│   │   └── poetry_tang.json    # 唐诗
│   ├── demo/                   # 测试接口
│   ├── divination/             # 起卦 / 占卜
│   ├── domain/                 # 域名相关
│   ├── douyin/                 # 抖音解析
│   ├── dynamic/                # 动态接口
│   ├── freeapi/                # 一言、头像、短码、B站、必应等
│   ├── ip/                     # IP 查询
│   ├── phone/                  # 手机号归属地
│   ├── spider/                 # 新闻/视频/历史/成语/诗词（本地种子数据）
│   ├── time/                   # 时间接口
│   ├── tools/                  # 哈希、Base64、UUID、密码、颜色、昵称
│   └── word/                   # 随机短句
├── core/
│   ├── config.py               # config.json 配置加载
│   ├── database.py             # SQLite 初始化与内置接口目录
│   ├── depends.py              # 公共依赖与鉴权
│   ├── exceptions.py           # 异常处理
│   ├── middleware.py           # 安全头、日志、限流等中间件
│   └── routing.py              # 路由索引
├── frontend/
│   ├── admin.html              # 后台页面
│   ├── doc.html                # 接口详情页
│   ├── index.html              # 首页
│   ├── register.html           # 注册页
│   └── assets/
│       ├── admin.js
│       ├── app.js
│       ├── doc.js
│       └── style.css
├── static/
│   ├── admin.css
│   ├── admin.js
│   ├── index.html
│   └── style.css
├── tests/
│   ├── conftest.py             # 测试隔离（把数据库指向临时目录）
│   └── test_app.py             # 自动化测试
├── tools/
│   └── build_datasets.py       # 语料构建
├── scripts/
│   ├── release.ps1             # 打包发行（Windows）
│   ├── release.sh              # 打包发行（Linux/macOS）
│   ├── smoke_test.py           # 基础接口冒烟
│   ├── smoke_spider_apis.py    # 爬虫类接口冒烟
│   ├── curl_public_check.py    # 公开接口 curl 检查
│   ├── mask_api_keys.py        # 日志 Api-Key 脱敏迁移
│   ├── fix_corpus_data.py      # 语料订正（引号 / 转码符号）
│   └── clean_test_pollution.py # 清理真实库里的测试污染行
├── deploy/
│   ├── api-platform.service
│   ├── nginx.conf.example
│   └── nginx.production.conf.example
├── Dockerfile
├── docker-compose.yml
├── config.json.example
├── README.md
├── DEPLOY.md
├── TERMUX.md
├── LICENSE
├── pytest.ini
└── requirements.txt
```

## 说明

- 主入口统一使用 `main.py`
- 路由注册统一放在 `apis/main.py`
- 配置统一使用 `config.json`
- 静态文案和结构化种子数据统一放在 `apis/data/`
- `route.py` 里尽量只保留接口逻辑，不再堆静态数据

## 默认开发凭据

```text
Api-Key: test123
Admin-Token: admin888
```

> ⚠️ **这两个值是公开的默认值，仅用于本地开发。**
> 服务一旦监听非回环地址（如 `0.0.0.0`），必须先把它们换成强随机值。
> `environment` 设为 `production` 时，程序会校验凭据强度，仍在使用默认值会**直接拒绝启动**。

## 生产环境

复制：

```bash
cp config.json.example config.json
```

然后修改 `config.json` 里的默认密钥和生产配置。

## 许可证

[MIT](LICENSE)
